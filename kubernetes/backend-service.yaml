apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: sticky-sessions-demo
  labels:
    app: backend-service
spec:
  selector:
    app: backend-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
