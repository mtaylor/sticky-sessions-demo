apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: sticky-sessions-demo
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
